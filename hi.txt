local toolbar = plugin:CreateToolbar("Xenith Anticheat")

local newScriptButton = toolbar:CreateButton("Xenith Anticheat", "Click to update the Anticheat", "rbxassetid://9387645418")

local SSS = game:GetService("ServerScriptService")
local StudioService = game:GetService("StudioService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")

local loggedInUserId = StudioService:GetUserId()
local loggedInUserName = Players:GetNameFromUserIdAsync(loggedInUserId)

print(game.CreatorId)

newScriptButton.Click:Connect(function()
	if SSS:FindFirstChild("Xenith Anticheat") then
		local anticheat = SSS:FindFirstChild("Xenith Anticheat")
		
		if not SSS:FindFirstChildWhichIsA("LocalScript") then
			local ui = script:WaitForChild("ScreenGui")
			ui.Parent = game:GetService("CoreGui")
			return;
		end
		
		game:GetObjects("rbxassetid://9373092659")[1].Parent = SSS
	else
		local ui = script:WaitForChild("ScreenGui")
		ui.Parent = game:GetService("CoreGui")
	end
end)
